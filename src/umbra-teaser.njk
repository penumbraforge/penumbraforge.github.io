---
layout: base.njk
title: "Umbra — Private local AI prototype | Penumbra Forge"
description: "Umbra is a private, unreleased local AI application being developed on Apple Silicon with MLX. A working prototype exists, but there is no public build or supported release yet."
permalink: /umbra/
---

{% include "header.njk" %}

<div class="umbra-page">

  <section class="umbra-hero">
    <span class="umbra-ghost" aria-hidden="true">☾</span>
    <div class="umbra-status">Private prototype — not publicly released</div>
    <h1>Umbra</h1>
    <p class="umbra-tagline">A private, unreleased local AI application being developed on Apple Silicon with MLX. A working prototype exists, but there is no public build or supported release yet.</p>
  </section>

  <section class="umbra-section">
    <h2>What exists today</h2>
    <p>The private development tree includes a browser interface, a Tauri desktop shell, a local FastAPI backend, MLX inference, chat, a workspace, plugins, and local knowledge features. These are parts of an unreleased development build, not a public product or support commitment.</p>
  </section>

  <section class="umbra-section">
    <h2>Current development environment</h2>
    <p>The current prototype is being developed on an <strong>Apple Silicon Mac with 32&nbsp;GB of memory</strong> and uses Apple MLX for local inference. That describes the machine and runtime in use today; there is no supported hardware matrix or reproducible performance claim.</p>
    <div class="umbra-pills">
      <span>Apple Silicon</span>
      <span>MLX</span>
      <span>32 GB development machine</span>
      <span>browser + Tauri interface</span>
      <span>private prototype</span>
    </div>
  </section>

  <section class="umbra-section">
    <h2>Status</h2>
    <p>A working private prototype exists. It is not publicly released, packaged, or supported, and I do not have a release date.</p>
    <p>Questions can be sent to <a href="mailto:shadoe@penumbraforge.com">shadoe@penumbraforge.com</a>.</p>
    <div class="umbra-actions">
      <a href="/about/" class="umbra-btn">About Penumbra Forge</a>
      <a href="/tools/" class="umbra-btn umbra-btn--ghost">Browser AI tools</a>
    </div>
  </section>

</div>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "Umbra",
  "applicationCategory": "DeveloperApplication",
  "description": "Private, unreleased local AI application being developed on Apple Silicon with MLX. A working prototype exists, but there is no public build or supported release yet.",
  "operatingSystem": "macOS",
  "url": "{{ site.url }}/umbra/",
  "author": { "@type": "Organization", "name": "Penumbra Forge", "url": "{{ site.url }}" }
}
</script>

<style>
.umbra-page {
  max-width: 1000px;
  margin: 0 auto;
  padding: 48px clamp(24px, 4vw, 48px) 96px;
}

.umbra-hero {
  position: relative;
  padding: 56px 0 32px;
}

.umbra-ghost {
  position: absolute;
  top: 8px;
  right: 0;
  font-size: 72px;
  opacity: 0.08;
  pointer-events: none;
}

.umbra-status {
  display: inline-block;
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--accent);
  border: 1px solid rgba(184, 165, 214, 0.3);
  border-radius: 100px;
  padding: 5px 12px;
  margin-bottom: 20px;
}

.umbra-hero h1 {
  font-family: var(--font-display);
  font-size: clamp(44px, 8vw, 72px);
  font-weight: 200;
  color: var(--text-1);
  line-height: 1;
  margin-bottom: 18px;
}

.umbra-tagline {
  font-size: 17px;
  line-height: 1.7;
  color: var(--text-2);
  max-width: 62ch;
}

.umbra-section {
  margin-top: 52px;
}

.umbra-section h2 {
  font-family: var(--font-display);
  font-size: 22px;
  font-weight: 400;
  color: var(--text-1);
  margin-bottom: 14px;
}

.umbra-section p {
  font-size: 15px;
  line-height: 1.75;
  color: var(--text-2);
  margin-bottom: 12px;
}

.umbra-section p { max-width: 70ch; }

.umbra-section strong {
  color: var(--text-1);
  font-weight: 500;
}

.umbra-section a {
  color: var(--accent);
  text-decoration: none;
}

.umbra-section a:hover {
  color: var(--accent-strong);
}

.umbra-pills {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 18px;
}

.umbra-pills span {
  font-family: var(--font-mono);
  font-size: 11.5px;
  color: var(--text-2);
  border: 1px solid rgba(184, 165, 214, 0.18);
  border-radius: 100px;
  padding: 6px 12px;
}

.umbra-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  margin-top: 24px;
}

.umbra-btn {
  display: inline-block;
  font-size: 13px;
  padding: 10px 18px;
  border-radius: 8px;
  background: var(--accent);
  color: var(--surface-0) !important;
  text-decoration: none;
}

.umbra-btn--ghost {
  background: transparent;
  border: 1px solid rgba(184, 165, 214, 0.3);
  color: var(--text-2) !important;
}

@media (max-width: 480px) {
  .umbra-page { padding: 32px 18px 72px; }
  .umbra-hero { padding: 32px 0 16px; }
  .umbra-ghost { font-size: 52px; }
}
</style>
